
// Bill = Tenemos un array con los platos consumidos
// K = a lo que no comió Ana
// B = a lo que Brian le cobró a Ana

 

function CalcularAna (bill, k, b){
    let totalAna = 0;
    for (let i =0; i < bill.length; i++) {
        if( i !== k){
        total += bill[i]
        }
    }

    // Lo que tiene que pagar Ana
    let x = Math.floor(totalAna/2);

    if (x === b) {
        console.log(CalcularAna);
    }
    else {
        console.log(b- x);
    }
}

//